version: 2.1
jobs:
  Release:
    docker:
      - image: node:11.8.0-alpine
    steps:
      - run:
          name: Release
          command: |
            helm upgrade health-care-server ./

workflows:
  version: 2
  default_job:
    jobs:
      - Release
